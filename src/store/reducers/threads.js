import { GET_THREADS } from '../constants'

const initialState = {}

export default function threads(state = initialState, action) {
    switch (action.type) {

        default:
            return state
    }
}
